# Knowledge base

This repository contains snippets used across our portfolio

The format for snippets is Markdown for easy editing and clean readability.

`snippets.md` contains language-agnostic helpers shared across all projects.

There are also language specific references - helpers for particular languages.
